import { GameView } from "@/components/base/game-view";

// Live interactive game view — the real RimWorld-style sim with canvas,
// moving agents, activity log, click-to-inspect. Kept here while /
// hosts the design mockup.
export default function PlayPage() {
  return <GameView />;
}
